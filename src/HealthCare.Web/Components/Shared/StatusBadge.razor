@namespace HealthCare.Web.Components.Shared
@using HealthCare.Web.Design

<Tag Color="@ToneColor">@Text</Tag>

@code {
    [Parameter]
    public string Text { get; set; } = string.Empty;

    [Parameter]
    public StatusTone Tone { get; set; } = StatusTone.Default;

    private string ToneColor => Tone switch
    {
        StatusTone.Success => "success",
        StatusTone.Warning => "warning",
        StatusTone.Error => "error",
        StatusTone.Info => "processing",
        StatusTone.Primary => "cyan",
        StatusTone.Neutral => "default",
        _ => "default",
    };
}
